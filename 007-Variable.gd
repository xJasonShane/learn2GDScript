// 将实体顺时针旋转0.3个弧度
func run():
    rotate(0.3)

// 将上述实体的旋转调整为0(初始状态)，这样做的好处是不需要知道当前实体具体的旋转角度，直接调整为初始状态
func run():
    rotation = 0

// 使用一个点号访问子变量
func run():
    position.x = 200
    position.y = 250

// 使用子变量将实体的缩放调整为1.5倍
func run():
    scale.x = 1.5
    scale.y = 1.5